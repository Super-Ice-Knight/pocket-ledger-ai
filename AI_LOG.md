# AI 协同开发日志

项目名称：口袋记账 AI 版

本日志按照真实开发过程整理。两个核心 Prompt 与一段补充对话保留原文或用户确认后的整理版，其余内容使用第一人称记录判断、取舍、Debug 和验收过程。

## 一、项目目标与人机责任

项目目标是做出能够通过自然语言快速记账、统计收支、管理预算并生成财务分析的完整 Web 产品。

开发过程中，我负责提出需求、决定产品方向、选择或否决方案、管理 API Key，并检查最终结果是否符合任务要求。Codex 主要负责读取仓库、分析方案、生成和修改代码、解释复杂逻辑、构造测试以及排查日志。产品内部的大模型只负责把自然语言转换为结构化草稿，或者根据统计数据生成财务点评，不能直接修改数据库。

我没有把“AI 生成后能运行”当作完成。金额精度、数据库写入、模型失败兜底、非法输入、时间处理和公开设置安全等核心逻辑，都需要经过代码检查、自动化测试和浏览器验收。最终代码责任仍然由我承担。

## 二、工具链与方案选择

本项目主要使用 Codex / ChatGPT 协助开发。
前端使用 Vite、React、TypeScript、Tailwind CSS 和 Recharts；
后端使用 FastAPI、Pydantic、httpx 和 SQLite；版本管理使用 Git 和 GitHub；
前端部署在 Vercel，后端部署在 Render；
验证工具包括 pytest、Playwright 和 Vite 生产构建。

项目开始时我考虑过 Streamlit，因为它主要使用 Python，学习成本较低。但实际页面更接近工具原型，难以达到我希望的产品质感。因此我否决了只使用 Python 的方案，选择 React 与 FastAPI 前后端分离。

视觉上，我也没有接受普通蓝白后台模板，而是要求使用左侧导航、独立页面切换、大理石材质和西方雕塑艺术风格。最终形成总览、AI 快记、流水、分析、预算和设置六个页面。

数据库选择 SQLite。它不需要单独安装数据库服务，适合单用户演示，也能保证刷新页面后数据不会消失。金额统一转换为整数分保存，例如 12.60 元保存为 1260 分，避免直接使用浮点数导致 0.1 + 0.2 出现精度误差。

## 三、核心 Prompt

选拔指南要求展示 1～2 个用于解决复杂逻辑的核心 Prompt。本日志保留两个核心 Prompt，并额外增加一段与主备接口和线上延迟有关的连续对话。

### 核心 Prompt 1：代码交付前静态审查

> 你现在是一名负责软件最终交付的高级代码审查专家。请基于当前仓库的真实状态，对“口袋记账 AI 版”进行一次提交前静态审查。
>
> 这次任务只做审查，先不要修改代码，不要提交、推送或部署，也不要调用会消耗额度的外部 AI 接口。你可以阅读 Git 状态、近期差异、任务书、README、AI_LOG、前后端源码、测试文件和部署配置。
>
> 审查目标不是寻找格式问题，而是判断项目是否存在会影响选拔评分、真实使用、数据正确性、安全性和答辩解释的缺陷。请重点检查以下内容：
>
> 1. 对照选拔指南，确认金额、分类、账户、时间、备注、月度流水、周度流水、统计、AI 快记、预算和财务点评是否真正实现，不要把相似功能当成已经满足要求。
> 2. 检查金额是否始终使用整数分存储，0 元、负数、超大金额和非法文本是否受到前后端共同限制，SQLite 创建、更新、删除、统计和缓存是否一致。
> 3. 检查新建与编辑是否都允许修改记账时间，前后端是否统一使用 Asia/Shanghai，月度、周度和日期分组是否依据同一业务时间计算。
> 4. 检查 AI 是否只生成草稿，模型输出是否经过 JSON 解析、字段归一化和数据校验，主模型、备用模型和本地兜底是否按顺序工作，缺失字段是否会阻止错误入账。
> 5. 检查财务点评是否采用手动生成和 SQLite 缓存，普通 GET 请求是否可能调用 Provider。
> 6. 检查修改自然语言后旧草稿是否失效，月份、周次和点评请求是否存在旧响应覆盖新页面的竞态，加载、错误、删除确认和取消编辑状态是否完整。
> 7. 检查仓库是否包含 API Key、数据库、.env、本机绝对路径或其他敏感信息，公开设置接口是否可能回显或修改 Key。
> 8. 检查现有测试是否覆盖核心风险，并找出“测试虽然通过，但真实使用仍可能失败”的位置。不要为了代码更漂亮提出大规模重构。
>
> 以下能力已经验证，审查时应作为不可回归项：金额使用整数分；AI 只生成草稿，用户确认后才入账；周统计按周一至周日计算；AI 点评采用手动生成和 SQLite 缓存；点评 GET 不调用 Provider；公开设置页面只读且不返回 API Key；桌面和手机六个页面没有横向溢出。
>
> 输出时先报告 Git 状态、审查范围和基线。Findings 必须放在最前面，并按 P0 严重、P1 高、P2 中、P3 低排序。每项问题都要包含文件和函数位置、触发条件、实际影响、代码证据、最小修复方案和需要增加的测试。明确区分已经确认的问题、根据代码推断的风险和无法静态确认的事项。
>
> 不要重复已经解决的问题，不要为了数量罗列普通格式问题。最多给出 5 项现在值得修复的内容，并说明评分价值、成本和回归风险。最后给出“可以交付、修复后交付或暂不建议交付”的结论，以及后续动态验收清单。如果没有发现严重问题，要明确说明剩余测试盲区，不能为了显得有工作量而虚构缺陷。

这段 Prompt 的重点是把 AI 限定为代码审查专家，而不是让它直接继续开发。它同时规定了审查范围、不可回归项、证据格式、严重等级和停止条件，避免 AI 只给出泛泛的“可以优化代码结构”。

静态审查发现了几个普通成功路径测试不容易暴露的问题：数据库保存了时间，但表单没有可编辑入口；0 元流水可以写入；修改自然语言后仍能提交旧 AI 草稿；快速切换月份时旧请求可能覆盖新页面。我没有把审查建议全部执行，而是根据评分价值、实现成本和回归风险选择需要修复的项目，再进入实现和动态测试。


### 核心 Prompt 2：AI 点评缓存决策


> 不要每一次刷新都调用api，ai点评应该存起来，如果没有变动的话直接展示，避免每一次都调用，并且在用户不查看点评的时候不用去调用api，如果没有好的自动逻辑的话就改成手动，或者采用混合逻辑，你来给出一个最优的方案。

这段 Prompt 来自实际使用中发现的成本问题。如果每次刷新、切换月份或切换语气都调用模型，不仅速度慢，也会重复消耗额度。AI 比较了完全自动、完全手动和混合三种方案，最终采用混合逻辑：进入页面时只读取 SQLite 中的点评缓存；只有用户点击生成或重新分析时才调用模型；账单没有变化时直接展示原结果，数据变化后保留旧结果但标记为需要更新。

后端使用账单统计、月份、语气、模型配置和 Prompt 版本生成指纹，返回 missing、fresh 或 stale 状态。测试还要求普通 GET 请求绝对不能调用 Provider，模型失败产生的 error_fallback 不能写入缓存。

### 补充 Prompt 3：主备接口与线上延迟


> 我觉得可以再做一个备用apikey，如果说调用模型的时候遇到了错误，就尝试调用备用模型，这个要求你觉得合理吗？然后把设置里的api配置直接做成真实设置。
>
> 为什么线上部署之后对于ai的访问会超级慢，测试都是一万和四千的ms，ai快记更是直接卡到本地兜底，是线上部署的问题还是我的网络的问题？

第一轮需求建立了主模型、备用模型和本地兜底链路，并把设置页从静态展示改成真实配置。第二轮没有继续盲目增加超时时间，而是要求判断延迟究竟来自浏览器、Render、模型平台还是代码逻辑。

最终诊断方法是分别测试健康接口、普通后端接口、Provider 测试和真实快记，并使用后端计时区分浏览器网络与模型请求耗时。结果证明 FastAPI 本身响应正常，主要问题来自 Render 部署区域到部分亚洲模型平台的链路和服务延迟。

## 四、开发过程中的主要取舍

AI 快记采用“模型只生成草稿，用户确认后写入数据库”的边界。我拒绝让模型自动入账，因为即使模型返回了合法 JSON，也可能错误理解金额、账户或时间。后端会再次进行字段归一化和 Pydantic 校验，前端还会显示缺失字段。

主备 Provider 机制得到保留，但我没有为了展示备用能力，把已经确认延迟很高的接口继续放在公开演示关键链路中。备用接口只有在真实验证可用后才配置；主备都失败时返回本地规则草稿，手动记账仍然可用。

财务点评从最初的本地固定文案改为 AI 分析，模型输出一句话结论、详细分析和行动建议。但为了控制成本，又增加了手动生成和 SQLite 缓存，而不是每次打开页面都重新生成。

对于设计 Skill、计划模式和测试流程，我也没有把工具名称当作能力证明。产品范围和交付优先级不明确时先规划；金额、时间、缓存和请求竞态等明确风险使用行为测试；页面布局、手机适配和真实交互再用 Playwright 与浏览器检查。开发循环是“需求判断、方案比较、实现、测试、浏览器验收、再次修正”，而不是一次 Prompt 后直接接受结果。

## 五、Debug 经历一：Render 构建失败

第一次部署 FastAPI 后端时，Render 在安装依赖阶段失败，关键日志包括：

~~~text
Using Python version 3.14.3
pydantic-core
maturin failed
Read-only file system
metadata-generation-failed
~~~

我把完整日志交给 AI，并要求先区分构建、启动和运行三个阶段。分析后发现，错误发生在安装依赖阶段，不是 FastAPI 启动命令或端口问题。

Render 默认使用 Python 3.14，而项目中的 pydantic-core 当时没有对应的预编译 wheel，因此 pip 转而尝试使用 Rust 和 maturin 从源码构建。构建环境中的 Cargo 缓存目录只读，最终导致 metadata-generation-failed。

本地项目已经在 Python 3.11 下通过验证，所以我没有盲目升级整个依赖树，而是在 .python-version 和 Render 配置中统一指定 Python 3.11.9。重新部署后依赖安装成功，健康接口返回：

~~~json
{"ok": true}
~~~

这次 Debug 让我认识到，部署失败不能只看日志最后一行。首先要确认失败阶段，再寻找最早出现的有效异常。看到 pip 报错也不代表一定要升级 pip 或全部依赖。

## 六、Debug 经历二：线上 AI 严重超时

后端部署成功以后，AI 快记依然非常慢。设置页测试经常需要数秒甚至几十秒，真实快记还会等待很久后进入本地兜底。

我先预热 Render，再分别测试健康接口、设置接口、Provider 测试和真实快记。健康接口和普通设置接口约为 200 毫秒，说明浏览器到 Render 以及 FastAPI 基础响应正常；但 Agnes 曾在约 45 秒后 ReadTimeout，硅基流动成功时也需要约 21 秒，一次真实快记等待约 50 秒后才由备用模型返回。

交换主备顺序仍然不能稳定解决问题，因此我没有继续通过放宽超时时间掩盖延迟，而是更换更适合当前部署区域的模型平台。

迁移到 Groq 时，第一次填写的模型名是 qwen3.6-27b，接口很快返回：

~~~text
HTTP 404: model_not_found
~~~

这说明 Base URL 和请求已经到达平台，问题不是网络或 Key 无效，而是模型标识错误。查询可用模型后，将名称修改为 qwen/qwen3.6-27b，调用恢复正常。

一次公开线上验收中，Provider 测试约 917 毫秒，两条真实快记分别约 3.64 秒和 4.87 秒，并返回 provider=primary。这些数据只是一次验收样本，不代表固定 SLA。

这个过程还说明“接口连接测试成功”不等于完整业务成功。AI 快记还要经过模型生成、JSON 解析、字段归一化、Pydantic 校验和前端状态更新，因此 Provider 测试与真实快记都必须验收。

## 七、核心逻辑与异常输入

金额全部以整数分进入数据库和统计，前端允许用户逐步输入 50.、50.0 和 50.05，提交时再转换。0 元、负数和超过最大范围的金额会被前后端共同拒绝。AI 草稿可以暂时用 0 表示没有识别出金额，但不能通过创建接口写入数据库。

本地规则不会直接取句子中的第一个数字，而是优先识别“50 元、50 块”等明确金额，其次识别“花了 50、收入 2000”等金额语境。多个金额、外币、千位分隔符、收入与支出冲突以及无法唯一确定的日期都不会强行猜测，而是标记缺失字段，要求用户确认。

例如“股票收益两千元，健身房办卡一千元”同时包含收入和支出，系统不应该自动相抵后保存为一笔账。正确做法是提示存在多笔交易，让用户拆分记录。中文数字主要交给模型理解，本地确定性规则无法确认时宁可阻止保存，也不伪造结果。

记账时间统一按 Asia/Shanghai 处理。后端将时间归一为带 +08:00 的 ISO 8601，前端使用专门函数在 API 时间和 datetime-local 之间转换。新建、AI 草稿和编辑流水都使用同一个可编辑时间字段，移动到其他日期、周或月份后，对应统计会同步变化。

用户修改已经解析过的自然语言时，原草稿会被标记为过期，确认按钮被禁用，必须重新解析。这样可以避免新描述与旧结构化草稿不一致。

API Key 只保存在后端环境变量中，不提交到 GitHub，也不会通过公开设置接口返回。公开演示环境中的设置为只读状态，访问者不能修改服务器配置。主模型失败后尝试备用模型，主备都失败时返回 error_fallback，但手动记账仍可使用。

## 八、测试与验收

项目最终通过 35 项后端 pytest 测试、7 项 Playwright 前端测试和 Vite 生产构建。测试覆盖整数金额、非法金额、北京时间跨日、编辑后跨周和跨月统计、AI 主备调用、模型失败兜底、点评缓存、旧草稿失效、月度请求竞态和歧义输入。

浏览器验收检查了 1440×900 桌面宽度和 390×844 手机宽度。六个页面均能正常切换，没有横向溢出。手机端能够完成自然语言输入、解析、修改草稿和确认入账；删除需要确认，取消后不会修改数据；网络失败时保留原输入并显示可理解的错误。

Git 历史按照项目阶段保存了初始化、AI 解析、预算看板、部署修复、点评缓存、周流水、竞态修复和最终验收等提交。版本管理不只是把最终代码上传一次，而是能够追踪主要问题在什么时候、为什么被修改。

## 九、协作总结

这次项目让我认识到，AI 协作不是写一个很长的 Prompt 后等待结果，而是一个持续循环：

~~~text
提出需求 → AI 给出方案 → 我进行取舍 → 实现代码
→ 测试验证 → 发现问题 → 查看日志 → 再次修正
~~~

Prompt 的价值也不只在于写得详细，而在于是否提供了角色、背景、边界、证据要求和验收标准。代码审查 Prompt 负责发现风险，缓存 Prompt 负责解决调用时机和成本问题，主备接口对话则推动了真实网络故障的诊断。

AI 提高了实现和排查效率，但产品方向、风险判断和最终责任仍然属于开发者。对我来说，最重要的不是 AI 生成了多少代码，而是我能否解释为什么选择这个方案、数据怎样流动、接口失败后怎样处理、非法输入为什么不会进入数据库，以及测试如何证明核心功能没有回归。
